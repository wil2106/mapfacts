import { Text, View } from "react-native";

export default function FactText({ fontSize, text }: { fontSize: number, text: string; }) {
  const width = (fontSize * 198.62) / 30;
  const height = (fontSize * 24.8) / 30;
  return <></>;
}
